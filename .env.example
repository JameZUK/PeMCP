# PeMCP Environment Configuration
# Copy this file to .env and fill in your values.
# The .env file is git-ignored and will not be committed.

# VirusTotal API key (optional, enables VT lookups)
VT_API_KEY=

# Bearer token for HTTP mode authentication (optional, recommended for network exposure)
PEMCP_API_KEY=

# Port to expose the HTTP MCP server on (default: 8082)
PEMCP_PORT=8082

# Path to the samples directory on the host (default: ./samples)
PEMCP_SAMPLES=./samples

# Path to the output directory on the host (default: ./output)
PEMCP_OUTPUT=./output

# Path to Qiling rootfs on the host (default: ./qiling-rootfs)
PEMCP_ROOTFS=./qiling-rootfs
