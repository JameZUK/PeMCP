# Arkana Dependencies
#
# These are minimum version constraints (>=) for development flexibility.
# For reproducible production/CI builds, generate a lockfile:
#   pip install -r requirements.txt
#   pip freeze > requirements.lock
# Then install from the lockfile: pip install -r requirements.lock

# --- Core PE Parsing ---
pefile>=2024.8.26

# --- Advanced Binary Analysis & Emulation ---
angr>=9.2
vivisect>=1.1.0
# FLIRT signature parsing (used by angr's built-in FlirtAnalysis)
nampa>=0.1

# --- FLARE Tools (Capabilities & Advanced Strings) ---
flare-floss>=3.0
# NOTE: the bundled capa-rules version is pinned in arkana/constants.py via
# CAPA_RULES_ZIP_URL (currently v9.4.0). capa 9.3.x and 9.4.x are both
# backward-compatible with the v9.4.0 rule set — the capa 9.4.0 release
# notes document no breaking rule-syntax changes, and the Rule.from_yaml
# loader has no minimum-capa-version check. Bumping either capa or the
# rules URL independently is safe; they do not need to be in lockstep.
# When bumping CAPA_RULES_ZIP_URL, existing users auto-invalidate via the
# ``.capa_rules_url`` marker file written by ensure_capa_rules_exist.
flare-capa>=9.3,<9.5

# --- String Analysis & Ranking ---
# PyPI package name is "stringsifter", NOT "flare-stringsifter"
stringsifter>=3.0
joblib>=1.2
numpy>=1.24
rapidfuzz>=3.0

# --- Data Serialization ---
# YAML parsing (C2 signature knowledge base)
PyYAML>=6.0

# --- Signatures, Cryptography & Scanning ---
yara-python>=4.3
signify>=0.7
cryptography>=43.0

# --- Network & API (VirusTotal/Updates) ---
requests>=2.28

# --- Server Infrastructure ---
# Model Context Protocol SDK
mcp[cli]>=1.0
# Template engine (for dashboard)
Jinja2>=3.1
# ASGI framework (dashboard web app)
starlette>=0.27
# Process-level resource monitoring (RSS/CPU)
psutil>=5.9

# --- Graph Analysis (used directly for angr CFG processing) ---
networkx>=3.0

# --- Extended Binary Analysis (Optional - graceful degradation) ---
# Multi-format binary parsing and modification (PE/ELF/Mach-O)
lief>=0.15
# Multi-architecture disassembler (standalone shellcode analysis)
capstone>=5.0
# Multi-architecture assembler (patch generation from mnemonics)
keystone-engine>=0.9
# Windows API emulation for malware analysis
speakeasy-emulator>=1.0
# Automatic PE unpacking (UPX, ASPack, FSG, etc.)
unipacker>=1.0
# .NET PE metadata extraction (latest PyPI: 0.2.x)
dotnetfile>=0.2
# Fuzzy hashing for sample similarity
ppdeep>=20200401
# Locality-sensitive hashing
py-tlsh>=4.7
# Embedded file/firmware detection
# Primary: system binwalk package (apt-get install binwalk) — always works via CLI fallback.
# Optional: binwalk3 pip package wraps the Rust v3 binary for faster Python API, but
# requires the v3 Rust binary to be installed separately (not bundled on Linux).
# binwalk3>=3.1

# --- .NET Binary Analysis (Optional) ---
# .NET PE metadata parser (CLR headers, metadata tables, streams)
dnfile>=0.18.0
# .NET CIL bytecode disassembler (Mandiant FLARE)
dncil>=1.0

# --- Go Binary Analysis (Optional) ---
# Go binary reverse engineering (compiler version, packages, symbols)
pygore>=0.5

# --- AutoIt3 Script Decryption (Optional) ---
# AutoIt compiled script decompression and bytecode deassembly
# Core decryption (MT19937 + RanRot PRNG) is built into arkana/parsers/autoit.py
# autoit-ripper provides LZSS decompression and bytecode→source conversion
autoit-ripper>=1.2

# --- Rust Binary Analysis (Optional) ---
# Rust binary metadata extraction (compiler, crates, toolchain)
rustbininfo>=0.5
# Rust symbol demangling
rust-demangler>=1.0

# --- Binary Data Transformation (Optional) ---
# Composable binary transforms: encoding, crypto, compression, PE ops, deobfuscation
binary-refinery>=0.9

# --- ELF/Linux Binary Analysis (Optional) ---
# ELF and DWARF parser (headers, symbols, debug info)
pyelftools>=0.31

# --- Test Dependencies ---
# Install with: pip install -r requirements.txt -r requirements-test.txt
# pytest
# httpx
